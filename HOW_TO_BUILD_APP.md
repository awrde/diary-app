# 📱 안드로이드 앱 빌드 및 출시 가이드

## 🚦 현재 진행 상황 (2025.12.13 업데이트)
- ✅ **개발 환경 구축 완료**: PC(Web) 및 Android 프로젝트 연동 완료
- ✅ **앱 빌드 성공**: `app-release.aab` 파일 생성 테스트 완료
- ⏳ **Google Play Console 신원 확인 중**: 이 과정이 끝나야 앱 업로드가 가능합니다. (보통 1~2일 소요)
- 👉 **다음 할 일**: 신원 확인이 승인되면, 생성한 `.aab` 파일을 Play Console의 '비공개 테스트' 또는 '프로덕션' 트랙에 업로드하세요.

---

이 문서는 **AI 일기장 웹 프로젝트**를 **안드로이드 앱(APK)**으로 변환하여 Play 스토어에 출시하는 방법을 설명합니다.

## 🚀 워크플로우: 코드 수정 후 앱 업데이트 순서

웹사이트 코드를 수정하고 앱에 반영하고 싶을 때마다 아래 순서대로 실행하세요.

1. **PC에서 코드 수정 및 테스트**
   - 평소처럼 `npm run dev` 로 개발 서버를 켜서 수정하고 확인합니다.

2. **웹 프로젝트 빌드 (정적 파일 생성)**
   - 앱으로 만들 준비를 합니다. (HTML/CSS/JS 파일 생성)
   ```bash
   npm run build
   ```

3. **안드로이드 프로젝트와 동기화**
   - 생성된 웹 파일을 안드로이드 프로젝트 폴더로 복사합니다.
   ```bash
   npx cap sync
   ```

4. **안드로이드 스튜디오에서 열기 & APK 생성**
   - 아래 명령어로 안드로이드 스튜디오를 엽니다.
   ```bash
   npx cap open android
   ```
   - 안드로이드 스튜디오가 열리면:
     1. 상단 메뉴 `Build` -> `Generate Signed Bundle / APK` 클릭
     2. `Android App Bundle` (Play 스토어 출시용) 또는 `APK` (테스트용) 선택
     3. 안내에 따라 키(Key)를 생성하고 빌드하면 끝!

---

## 🛠️ 초기 설정 (이미 완료됨)
- Capacitor 설치 및 초기화
- `next.config.mjs` 설정 (`output: 'export'`)
- Android 플랫폼 추가 (`npx cap add android`)

## 💡 꿀팁
- **아이콘 변경**: `android/app/src/main/res` 폴더 안의 `mipmap` 폴더들에 있는 아이콘 이미지(`ic_launcher`)를 교체하면 앱 아이콘이 바뀝니다.
- **앱 이름 변경**: `android/app/src/main/res/values/strings.xml` 파일에서 `<string name="app_name">AiDiary</string>` 부분을 수정하세요.
